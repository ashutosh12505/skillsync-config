server:
  port: 8084

spring:
  application:
    name: skill-service

  datasource:
    url: jdbc:mysql://localhost:3306/skillsync_skill
    username: javauser
    password: password

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
