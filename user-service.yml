server:
  port: 8082

spring:
  application:
    name: user-service

  datasource:
    url: jdbc:mysql://localhost:3306/skillsync_user
    username: javauser
    password: password

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
