server:
  port: 8085

spring:
  application:
    name: session-service

  datasource:
    url: jdbc:mysql://localhost:3306/skillsync_session?useSSL=false&serverTimezone=UTC
    username: javauser
    password: password

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
