server:
  port: 8081

spring:
  application:
    name: auth-service

  datasource:
    url: jdbc:mysql://localhost:3306/skillsync_auth
    username: javauser
    password: password

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
